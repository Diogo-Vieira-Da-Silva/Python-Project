The development process was not as difficult as I initially expected, mainly because the language has a syntax similar to JavaScript. I only needed some time to adapt and understand new functions and methods, which allowed me to successfully complete the tasks.

To run the files, simply use the python command followed by the file name in the terminal, for example:

python atividade1.py

-----------------------------------Tradução--------------------------------------------

O desenvolvimento não foi tão difícil quanto eu inicialmente imaginava, principalmente por se tratar de uma linguagem com sintaxe semelhante ao JavaScript. Foi necessário apenas um período de adaptação para compreender novas funções e métodos, o que me permitiu concluir as atividades com sucesso.

Para executar os arquivos, basta utilizar o comando python seguido do nome do arquivo no terminal, por exemplo:

python atividade1.py